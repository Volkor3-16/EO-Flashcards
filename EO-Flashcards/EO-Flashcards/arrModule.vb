Module arrModule
    'This module reads the esperanto dictonary textfile and then puts them into two arrays, one for eo, and one for en.
    ' " : " as a seperator, so each of the words are working. 
End Module
